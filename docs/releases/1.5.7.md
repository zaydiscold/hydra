# Hydra 1.5.7 Patch Release

Date: 2026-06-02
Baseline: `v1.5.6`

## Summary

Hydra `1.5.7` is a narrow Account Generator rescue. It fixes the local
OTP-submit path that could look frozen after the operator entered a six-digit
code, removes the high-cost local limiter from OTP verification, and keeps the
Generator UI from showing stale OTP controls after the browser task has moved
into finalization.

The upstream OpenRouter/Clerk human-verification and email-code ownership
boundaries remain intact. Hydra now makes the local handoff clearer and more
responsive while the isolated browser completes the real session capture and
management-key bootstrap work.

## Changes

- Removes `highCostRouteLimiter` from `POST /api/generator/verify/:taskId`.
  Generator starts remain high-cost limited; submitting an OTP for an already
  running task is not treated as another expensive account-creation launch.
- Validates submitted OTPs as exactly six digits in the controller before the
  browser finalization path starts.
- Moves the renderer to `submitting_otp` optimistically before the quiet
  verification request returns, so Enter/Submit gives immediate visible
  feedback instead of leaving the form grey and ambiguous.
- Blocks stale OTP checkpoint rendering once the task is in finalization or a
  terminal state. A late `checkpoint.state=otp` can no longer keep the OTP form
  visible after submit.
- Makes duplicate OTP submits idempotent while finalization is already running.
  A second click/Enter returns success with "OTP is already being processed."
  instead of racing the browser state.
- Records `otpAcceptedAt` metadata when the server accepts a code, which gives
  logs and future diagnostics a concrete timestamp for the handoff boundary.
- Adds static regression coverage for the route limiter contract, strict OTP
  schema, optimistic renderer state, finalization-state OTP hiding, idempotent
  server submit, and `otpAcceptedAt` metadata.

## Verification

Local focused verification:

- `node --check server/controllers/GeneratorController.js`
- `node --check server/routes/generator.js`
- `node --check server/services/account-generator.js`
- `npm run test:background-failure-visibility` (`34/34`)
- `npm run test:ui-static` (`48/48`)
- `npm run lint`
- `npm run build`
- `npm run gate` (`12/12`)
- `npm test`
- `node bin/hydra.mjs audit --json`

Local macOS ARM package evidence:

- `npm run electron:build:mac-arm64`
- `HYDRA_BUILD_TARGET=darwin-arm64 npm run electron:smoke`
- `codesign --verify --deep --strict --verbose=2 release/mac-arm64/Hydra.app`
- Bundle version: `1.5.7`
- Zip: `release/Hydra-1.5.7-mac-arm64.zip`
- Zip SHA-256:
  `0dbda1ab09a220ec7a02c3a27aad287cfbe39b6ca1d7d35071fc6b3d2449e8d3`
- Embedded source inspection confirmed the packaged server contains
  `OTP_FINALIZATION_STATUSES`, strict six-digit OTP validation,
  `OTP is already being processed.`, `otpAcceptedAt`, and a verify route that
  no longer includes `highCostRouteLimiter`.
- Embedded renderer bundle inspection confirmed the packaged Generator page
  contains `OTP_FINALIZATION_STATUSES`, `submitting_otp`, and the optimistic
  submit path.
- Local notarization remains deferred because Apple signing credentials are not
  present; the local app is ad-hoc signed and strict codesign-valid for dogfood.

Live dev smoke:

- Started the patched dev app and opened `/generator`.
- Unlocked the local dev UI with the development password path; the temporary
  bearer token was removed after the run.
- Started a Generator task with a redacted supplied alias and supplied
  password.
- The direct FAPI signup path correctly fell back to the isolated browser when
  Clerk reported no matching account.
- The isolated browser filled the current OpenRouter signup form, accepted
  terms, clicked Continue, and reached the current upstream security-check /
  manual-verification boundary instead of the old generic
  `Timeout 30000ms exceeded` hang.
- The local Generator page rendered the active job state with equal-width
  **Show browser** and **Cancel job** buttons.
- The bounded smoke cancelled the task through `DELETE /api/generator/:taskId`;
  the log recorded the supervisor reason and the isolated Playwright profile
  was removed.

Evidence:

- Dev UI screenshot:
  `docs/evidence/hydra-v157-generator-dev-initial.png`
- Screenshot SHA-256:
  `a41c33c6c1eb766df52333ecddf09ad93bdbe3fb3e54b2595c465bf671bd95de`
- Earlier five-minute idle package profile completed at four Hydra-owned
  processes, zero stale profiles, `0.0%` average CPU, and RSS falling from
  `574832 KB` to `493152 KB`.

## Manual Boundary

This patch fixes Hydra's local OTP handoff and responsiveness. It does not
claim to bypass upstream security checks, retrieve the email code, approve
Turnstile/CAPTCHA, approve Touch ID, or complete a real account signup without
operator action.
