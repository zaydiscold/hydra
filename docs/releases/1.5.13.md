# Hydra 1.5.13

## What changed

- Restored the older diagonal shooting-star path so meteors streak across the shell instead of falling straight down.
- Repaired the lower-right planet accent with static guide rings and moving moon bodies, keeping the visual alive without a hot continuous loop.
- Fixed Command account balances so list, grid, cards, and map labels do not round fractional remaining credits up to `$20.00`.
- Added balance labels to Command map nodes, including low/depleted styling.
- Updated design and release documentation for the meteor geometry, planet scheduler, and balance-display contract.

## Verification

- Passed `npm run test:ui-static` with 49 UI/static contract checks.
- Passed `npm run build`.
- Rebuilt and smoke-tested the macOS ARM package.
- Verified strict deep codesign, packaged assets, and LaunchServices idle behavior on the local app bundle.
