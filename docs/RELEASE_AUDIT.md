# Hydra Release Audit

This is the current completion audit against `docs/CODEX_GOAL.md`.

Last updated: 2026-05-18 20:53 PDT.

## Objective

Ship Hydra as a polished full-stack Electron desktop app with working macOS and
Windows artifacts, plus the requested Docker/server runtime and Intel Mac path.

## Prompt-to-Artifact Checklist

This is the completion checklist for `docs/CODEX_GOAL.md`. A row is not complete
unless the evidence is current and directly covers the requirement.

| Goal requirement | Artifact or evidence | State |
| --- | --- | --- |
| Working polished Electron desktop app | macOS ARM and Intel packages build, smoke, and codesign; static UI/chrome/contrast/dead-button tests pass; source-level IPC and package contracts pass | Code-verifiable scope is current. Packaged GUI dogfood/screenshots plus Intel/Windows launch dogfood are deferred from the active Codex plan at the user's request. |
| Runs on macOS | `release/Hydra-1.0.0-mac-arm64.zip`, `release/Hydra-1.0.0-mac-x64.zip`, local ARM and x64 build/smoke/codesign evidence | ARM and x64 packages are current after the native titlebar fix. The Intel artifact was refreshed with a staged `chrome-mac-x64` Playwright cache, `HYDRA_BUILD_TARGET=darwin-x64 npm run electron:smoke` passed, `file release/mac/Hydra.app/Contents/MacOS/Hydra` reports `x86_64`, and `codesign --verify --deep --strict --verbose=2 release/mac/Hydra.app` passes. GUI launch flow still needs packaged dogfood |
| Runs on Windows | `.github/workflows/electron-smoke.yml` and `.github/workflows/release.yml` build Windows x64 NSIS on Windows runners with Node 22 and package smoke; `server/tests/workflow-contract.test.mjs` locks this workflow contract into `npm test`, including target-specific `HYDRA_BUILD_TARGET=win32-x64` Chromium smoke coverage | Windows x64 installer artifact is now present and smoke-verified: `release/Hydra-1.0.0-win-x64.exe` was built on macOS with an isolated `chrome-win` cache and `-c.win.signAndEditExecutable=false`, then `HYDRA_BUILD_TARGET=win32-x64 npm run electron:smoke` passed on 2026-05-18 16:37 PDT. Actual Windows launch/install dogfood still requires a Windows machine or runner. |
| Full-stack MVP features work | Unit/contract/CLI coverage for auth status, session refresh, dashboard data, proxy, codes preflight, guarded codes/keys/account ops, diagnostics, `/v1`, direct OpenRouter CLI probes, and shutdown | Code-verifiable coverage expanded; live OTP/redemption/real-key rotation and GUI flows are manual/deferred. |
| No dead buttons or silent IPC failures | `server/tests/ui-static-contract.test.mjs` dead-button guard; `server/tests/electron-ipc-contract.test.mjs`; renderer native calls route through `src/lib/native.js` | Verified by static/contract tests; real menu/button dogfood still pending |
| Packaged macOS window controls and drag | User reported on 2026-05-18 that the packaged app is visible but cannot be closed, moved, dragged from the top bar, or inspected for red/yellow/green traffic lights. `electron/app/windows.js` now uses a standard native macOS frame with no hidden-inset titlebar override, and `src/App.jsx` returns no renderer-owned AppChrome on Mac so AppKit owns the red/yellow/green controls and titlebar drag area. Renderer-owned window controls remain only for non-Mac platforms. `npm run test:electron-main-process`, `npm run test:ui-static`, `npm run lint`, `npm run build`, `npm run gate`, `ELECTRON_CACHE=/private/tmp/hydra-electron-cache npm run electron:build:mac-arm64`, `HYDRA_BUILD_TARGET=darwin-arm64 npm run electron:smoke`, and `codesign --verify --deep --strict --verbose=2 release/mac-arm64/Hydra.app` passed on 2026-05-18 16:24-16:28 PDT. The bundle plist/executable/helper layout was inspected on 2026-05-18 15:03 PDT: `CFBundleExecutable=Hydra`, `CFBundlePackageType=APPL`, main executable is arm64 Mach-O, helper `.app` bundles have matching executables, symlinks are intact, and `codesign --verify --deep --strict` passes. | Source/package verified; still requires final packaged Electron GUI dogfood/screenshot audit at the end, not browser/Vite |
| Packaged macOS launch crash report | User provided a 2026-05-18 crash report for Hydra 1.0.0 on macOS 26.1: `EXC_CRASH (SIGABRT)`, `abort() called`, faulting thread in `HIServices` `_RegisterApplication`, with `Parent Process: node`. Local inspection shows `release/mac-arm64/Hydra.app` has `CFBundleExecutable=Hydra`, `CFBundlePackageType=APPL`, `CFBundleIdentifier=com.zayd.hydra`, `PkgInfo=APPL????`, helper apps only under `Contents/Frameworks`, no nested `.app` under `Contents/Resources`, executable permissions on `Contents/MacOS/Hydra`, arm64 Mach-O binary, no `com.apple.quarantine` xattr in the inspected release bundle, and `codesign --verify --deep --strict` passes. `codesign -dv --verbose=4` reports `Format=app bundle with Mach-O thin (arm64)`, `Signature=adhoc`, `Info.plist entries=32`, and `Sealed Resources version=2`. `spctl --assess --type execute --verbose=4 release/mac-arm64/Hydra.app` returns `internal error in Code Signing subsystem` for the unsigned/ad-hoc local artifact, so Gatekeeper acceptance is not verified. The parent process indicates the crash path is consistent with a direct executable/automation spawn of `Contents/MacOS/Hydra`, not normal Finder/LaunchServices `.app` opening. `scripts/open-packaged-app.mjs` and `npm run electron:open:mac-arm64` now launch `open -n release/mac-arm64/Hydra.app` and warn against direct executable launches for GUI dogfood; on 2026-05-18 16:50 PDT the helper was tightened to preflight `CFBundlePackageType`, `CFBundleExecutable`, and executable permissions before calling `open`. The fresh run printed `bundle OK: CFBundlePackageType=APPL, CFBundleExecutable=Hydra` and `executable OK: .../Contents/MacOS/Hydra`, then LaunchServices still returned `NSOSStatusErrorDomain Code=-10827 kLSNoExecutableErr`; `open -n /System/Applications/Calculator.app` returned the same error, matching the earlier sandbox class and not changing the structural bundle evidence. Computer Use also still denied `com.zayd.hydra`, and `list_apps` did not show Hydra as running. | Crash is recorded as real packaged-launch evidence; final dogfood must retry via LaunchServices, not by spawning the app binary |
| No orphaned processes or runaway Playwright | Cleanup code and tests cover browser isolation, server shutdown contracts, Playwright profile isolation, and cross-platform auxiliary-process sweeps including Windows `taskkill` | Partly verified; packaged tray/quit/orphan-process audit still pending |
| `npm run electron:build` and `npm run electron:smoke` produce the product | ARM build/smoke passed locally; Intel x64 build/smoke/codesign passed locally with a staged x64 Playwright cache; Windows x64 NSIS artifact now exists and target smoke passes; workflows cover Windows/Linux package smoke | Verified for macOS ARM, macOS Intel/x64, and Windows x64 package contracts. Linux still needs runner execution evidence, and GUI dogfood remains final acceptance |
| Docker/server runtime works | Docker config passes; image build passed; Dockerfile uses Node 22 bookworm and skips Electron rebuild scripts; `scripts/docker-smoke.mjs` bounds Docker config/info/build/start/health checks with named timeouts, collects failed-start compose state/logs, and removes Created containers with `compose down --remove-orphans` | Image/config verified; local Docker daemon availability is an environment dependency, and a runtime container-start smoke still requires a host with Docker running |
| Private Hydra API map and closed-app CLI | `docs/hydra-api.openapi.json`, `npm run openapi:hydra`, `hydra api-map`, closed-app CLI commands, tests, and docs | Verified; Printing Press is methodology only. No upload, registration, public-library sync, public endpoint-tool generation, or shared catalog publishing path is part of release |
| Documentation stays current | `docs/RELEASE_AUDIT.md`, `docs/PACKAGED_ELECTRON_DOGFOOD.md`, `docs/HYDRA_API_MAP.md`, `docs/HYDRA_CLI_AND_AI_API_PLAN.md`, `docs/PACKAGING.md`, `docs/DOCKER.md`, `CLAUDE.md`, and `docs/AGENTS.md` updated | Verified for current non-GUI work |

## Verified

| Requirement | Evidence |
| --- | --- |
| Verification requirement captured | `docs/CODEX_GOAL.md` now includes `Verification Pass — Prove What Was Already Done` and requires current evidence before completion |
| Core test suite | `npm test` passed on 2026-05-16 16:39 PDT after wiring dormant tests, the bounded Docker smoke contract, workflow contract, TaskSupervisor cleanup logging, background failure visibility, Playwright automation soft-failure logging, production silent-catch cleanup, renderer cleanup/history/lifecycle failure visibility, renderer clipboard/probe/diagnostics/key-modal/menu feedback hardening, Dashboard/Pool Manager/Account Detail probe-failure visibility, local CORS origin narrowing, auth cookie parser hardening, filesystem fallback visibility, session lifetime probe failure visibility, Electron startup/runtime best-effort visibility including log tee write/close failure reporting, legacy data migration integrity, proxy gate fail-open visibility, proxy RequestLog fallback visibility, OpenRouter account snapshot fallback visibility, Pool key-validation response visibility, self-heal cleanup fallback visibility, Prisma disconnect failure visibility, local-secrets persistence fallback visibility, management-key unreadable-row visibility, native folder-open failure feedback contracts, corrupt-account purge logging, redemption-history read/write/alias logging visibility, magic-link cleanup lifecycle, magic-link callback fallback logging, account dedup/silent-refresh fallback logging, pool status/sync-key registration fallback logging, Debug vampire-mode profile preload fallback logging, dashboard session-status fallback logging, redemption fallback logging, schema-sync fallback logging, biometric fail-closed auth-token gating, startup failure action feedback, closed-app audit evidence expansion, first-run setup wizard coverage, test-chain completeness, and the Electron main-process/path-allowlist contracts into the main chain. It now runs every normal `server/tests/*.test.mjs` file plus the active `electron/tests/*.test.mjs` package-contract files, including `test:test-chain-completeness`, `test:openapi-map`, `test:ui-static`, `test:docker-smoke`, `test:workflow-contract`, `test:health-pinger`, `test:task-supervisor`, `test:background-failure-visibility`, `test:auth-cookie`, `test:gzip-middleware`, `test:error-boundary-sanitization`, `test:prisma-error-classification`, `test:phase1-backward-compat`, `test:management-key-backfill`, `test:db-proxy-cache`, `test:schema-self-heal-backups`, `test:schema-hash`, `test:electron-url-allowlist`, `test:local-secrets`, `test:electron-data-path`, Electron IPC/launch/data-path/main-process/path-allowlist/Prisma-asar contracts, API integration, browser isolation, SSE truncation, and CLI `api-map`/`account`/`accounts purge --dead`/`accounts sync --dry-run`/`ai chat`/`audit`/`codes`/`keys`/`keys provision --dry-run`/`keys rotate --dry-run`/`session`/`proxy`/`scan`/`export`/`import --dry-run`/`serve`/`stop`/`logs --tail`/`unlock` coverage; the run included the current 17-test UI static suite with dead-button, navigation, contrast, diagnostics-in-settings, compact generator, first-run setup wizard, Settings preference persistence, backend-down recovery guidance, upstream-offline cached-local-data messaging, clipboard failure feedback, native menu feedback toasts, app-shell lifecycle fallback visibility, renderer cleanup/history/probe failure visibility, primary-page AnimeText header coverage, and Anime.js splitText/addEffect guards |
| Full suite retest after biometric, renderer API errors, schema sync, upstream health, proxy, store, legacy storage, splash, OpenRouter automation, Playwright cleanup fallback visibility, dev-port cleanup hardening, utility/test cleanup visibility, and native macOS titlebar/traffic-light hardening | `npm test`, `npm run gate`, `npm run lint`, `npm run build`, focused Electron/UI/CLI/background/schema/upstream tests, package smoke, `git diff --check`, and `node bin/hydra.mjs audit --json` passed across the 2026-05-18 hardening pass. Target-specific smoke now rejects wrong-target Chromium archives, validates packaged app-shell files, verifies LaunchServices-relevant macOS plist fields, checks distributable artifacts, and keeps packaged GUI dogfood/manual screenshots out of code-verifiable completion. |
| Test-chain completeness | `server/tests/test-chain-completeness.test.mjs` parses `package.json`, follows nested `npm run` calls from `npm test`, and fails if any normal `server/tests/*.test.mjs` or `electron/tests/*.test.mjs` file is not reachable from the main suite. It is wired as `test:test-chain-completeness`, runs first in `npm test`, and passed inside full `npm test` on 2026-05-16 09:17 PDT. |
| Focused config/session retest | `npm run test:config`, `npm run test:webhooks`, and `npm run test:session-refresh-contract` passed on 2026-05-16 08:25 PDT after refreshing the goal-sheet status for config validation, JWT secret validation, Clerk session revocation, stacked `clientCookies`, and live-probe persistence |
| Focused engineering-review retest | `npm run gate`, `npm run test:electron-ipc-contract`, and `npm run test:local-secrets` passed on 2026-05-16 08:26 PDT after refreshing the goal-sheet status for IPC result handling, local-secrets corruption recovery, graceful shutdown, splash always-on-top, and portable Electron dev launcher contracts |
| SSE truncation regression coverage | `server/lib/sse-stream.js` forwards partial SSE bytes and appends `STREAM_INTERRUPTED` plus `[DONE]` when upstream closes without `[DONE]` or throws mid-stream; `npm run test:proxy-sse` passed on 2026-05-16 08:28 PDT with clean-completion, premature-close, and upstream-error cases |
| Proxy gate fail-open visibility | `server/services/proxy-gate.js` now logs unreadable or malformed persisted proxy-gate state before defaulting to enabled, so a disabled proxy cannot fail open after restart without warning evidence. `server/tests/proxy-gate.test.mjs` locks this down, and `npm run test:proxy-gate`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 13:51 PDT. |
| API integration tests are real server tests | `server/tests/electron-api-integration.test.mjs` now creates an isolated SQLite data dir, boots the real Express app on port 0, asserts `/api/auth/status`, protected `/api/accounts`, missing-key `/v1/models`, protected `/api/system/health` uptime/server-clock/PID/upstream fields, and embedded `/api/shutdown` contracts, then shuts down through `gracefulShutdown({ exit:false })`; `npm run test:api-integration` passed with 5 tests on 2026-05-16 08:44 PDT |
| Local CORS origin narrowing | Express CORS now accepts no-origin requests, exact same-origin app requests, the configured Vite dev origin outside production, and explicit `HYDRA_CORS_ORIGINS`, but rejects arbitrary loopback browser origins so any localhost page cannot reuse Hydra's host-scoped auth cookie just by choosing another port. `server/tests/electron-api-integration.test.mjs` covers same-origin/Vite acceptance and arbitrary-loopback rejection; `node --check server/index.js`, `node --check server/tests/electron-api-integration.test.mjs`, `npm run test:api-integration`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:33 PDT. |
| Auth cookie parser hardening | `server/middleware/auth.js` now catches malformed percent-encoded cookie values, logs a redacted warning naming only the cookie, and continues through normal token validation so bad cookie headers produce a normal 401 instead of a middleware exception. `server/tests/auth-cookie.test.mjs` covers extraction and `requireUnlocked` with malformed cookie input; `node --check server/middleware/auth.js`, `node --check server/tests/auth-cookie.test.mjs`, `npm run test:auth-cookie`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:36 PDT. |
| Browser isolation regression coverage | `server/tests/playwright-isolation.test.mjs` asserts default launches do not choose real Chrome, every managed `userDataDir` is fresh under the OS temp dir and never points at real Chrome/Chromium profile paths, required isolation flags are present, packaged mode extracts archived Chromium into userData, and stale profile sweep failures keep path-level warning evidence; `node --check server/lib/playwright-browser.js`, `node --check server/tests/playwright-isolation.test.mjs`, and `npm run test:browser-isolation` passed on 2026-05-17 03:26 PDT |
| Backend-down and invalid-response UI recovery contract | `src/api.js` raises explicit dev/prod backend-unavailable messages, exposes copyable `npm run dev` / `npm run server` guidance, keeps loading-state events balanced when fetch fails, preserves status/route/parse cause through `INVALID_API_RESPONSE`, and no longer mislabels malformed auth 401 responses as invalid credentials; `server/tests/ui-static-contract.test.mjs` locks this down |
| OpenRouter offline banner source contract | `src/App.jsx` polls `GET /api/system/health` while authenticated, renders `UpstreamStatusBanner` for offline/unknown upstream state, and tells the user cached local data remains available while proxy, provisioning, signup, OTP, and code redemption may fail; `server/tests/ui-static-contract.test.mjs` locks this down with banner CSS/accessibility checks, and `npm run test:ui-static` passed with 10 tests on 2026-05-16 08:52 PDT |
| Clipboard failure feedback | Account detail key copies, Pool Manager endpoint/model copies, created-key modal auto/manual copies, and bulk OTP export copying now await `navigator.clipboard.writeText()` and surface copied/copy-failed UI or toast feedback instead of empty catches/fire-and-forget writes; `server/tests/ui-static-contract.test.mjs` locks the active UI clipboard writes down, and `npm run test:ui-static` passed with 11 tests on 2026-05-16 09:03 PDT |
| Lint | `npm run lint` passed on 2026-05-18 17:02 PDT after replacing hidden-inset macOS chrome with standard AppKit titlebar ownership, adding Windows installer artifact evidence, refreshing macOS Intel artifact evidence, and fixing the stale CLI audit contract |
| Production web build | `npm run build` passed on 2026-05-18 17:06 PDT after the packaged dogfood runbook, audit, package-smoke, and docs consistency updates |
| Integration gate | `npm run gate` passed on 2026-05-18 19:38 PDT with 12 passed, 0 failed after the active-scope audit adjustment, Account Detail fallback-comment cleanup, and fresh production build |
| Current full test chain | Full `npm test` passed again on 2026-05-18 17:11 PDT after replacing hidden-inset macOS chrome with standard AppKit titlebar ownership, rebuilding the ARM package, producing/smoking the Windows installer artifact, fixing the Windows `.exe` smoke false negative, adding LaunchServices bundle preflight to the packaged app opener/audit contract, hardening package smoke resource selection/native-titlebar checks, and updating the dependency lockfile for `brace-expansion@5.0.6`. This included CLI, UI-static, workflow, Docker-smoke contract, Electron main-process, IPC, path-allowlist, schema, package-contract, and Prisma-asar coverage. |
| Current code-verifiable closeout | After the active scope was narrowed away from packaged GUI dogfood/screenshots and manual Intel/Windows launch work, `npm run test:cli`, `npm run test:ui-static`, `npm run build`, `npm run gate`, full `npm test`, `npm run lint`, `HYDRA_BUILD_TARGET=darwin-arm64 npm run electron:smoke`, `HYDRA_BUILD_TARGET=darwin-x64 npm run electron:smoke`, `HYDRA_BUILD_TARGET=win32-x64 npm run electron:smoke`, `git diff --check`, and `node bin/hydra.mjs audit --json` passed on 2026-05-18 19:33-19:45 PDT. `npm run test:ui-static`, `npm run test:background-failure-visibility`, `npm run test:cli`, `npm run lint`, `npm run build`, full `npm test`, `git diff --check`, `npm audit --json`, and `node bin/hydra.mjs audit --json` passed again on 2026-05-18 19:52-19:54 PDT after route-title cleanup, plan consolidation, import-order cleanup, and unused virtualization dependency removal. `npm run test:api-integration`, `npm run test:gzip-middleware`, `npm run test:workflow-contract`, `npm run test:cli`, `npm run lint`, and `git diff --check` passed on 2026-05-18 20:35-20:43 PDT after public-doc cleanup and CI hardening. GitHub `master` head `66f3e5ca27119f7290c5cd503657cb016e21abf9` passed CI (`npm ci`, lint, build, full `npm test`, gate, build) and Docker image build/push on 2026-05-18 20:43-20:52 PDT. `hydra audit --json` reports `complete: true`, `checked: 27`, `ok: 23`, `deferred: 4`, `missing: 0`, and `blockers: 0` for the active code-verifiable scope. |
| macOS ARM package contract | `ELECTRON_CACHE=/private/tmp/hydra-electron-cache npm run electron:build:mac-arm64`, `HYDRA_BUILD_TARGET=darwin-arm64 npm run electron:smoke`, and `codesign --verify --deep --strict --verbose=2 release/mac-arm64/Hydra.app` passed again on 2026-05-18 16:27-16:28 PDT after replacing hidden-inset macOS chrome with standard AppKit titlebar ownership and rebuilding the ARM package. Smoke confirmed `release/mac-arm64/Hydra.app` has `Info.plist`, `PkgInfo`, `CFBundleExecutable=Hydra`, `CFBundlePackageType=APPL`, `CFBundleIdentifier=com.zayd.hydra`, the main executable, helper executables, no nested `.app` bundles under `Contents/Resources`, the target-specific Chromium zip, `release/Hydra-1.0.0-mac-arm64.zip` containing `Hydra.app/Contents/MacOS/Hydra`, Prisma engine, empty DB, and package resources without launching the GUI. Codesign reported `valid on disk` and `satisfies its Designated Requirement`. Notarization remains skipped without Apple credentials. |
| macOS ARM artifact | `release/Hydra-1.0.0-mac-arm64.zip` exists at 285 MB with `release/Hydra-1.0.0-mac-arm64.zip.blockmap` at 305 KB after the 2026-05-18 14:54 PDT rebuild; `release/mac-arm64/Hydra.app/Contents/PkgInfo` exists |
| Codex macOS app-launch environment | `open -n release/mac-arm64/Hydra.app`, `open -n /private/tmp/HydraLaunchNoSpace.app`, and `open -n /System/Applications/Calculator.app` all returned `NSOSStatusErrorDomain Code=-10827 kLSNoExecutableErr` from this sandbox on 2026-05-18 15:01-15:04 PDT, and Computer Use was denied for `com.zayd.hydra`. Because the same LaunchServices error occurs for Apple's Calculator, this is recorded as a Codex/macOS app-launch environment blocker rather than Hydra bundle evidence. |
| macOS Intel package contract | `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/hydra-pw-cross ELECTRON_CACHE=/private/tmp/hydra-electron-cache ELECTRON_BUILDER_CACHE=/private/tmp/hydra-electron-builder-cache npm run electron:build:mac-x64` passed on 2026-05-18 16:44 PDT after staging Playwright's Chrome-for-Testing `145.0.7632.6` `chrome-mac-x64` payload. `HYDRA_BUILD_TARGET=darwin-x64 npm run electron:smoke` passed again on 2026-05-18 19:42 PDT, confirming the x64 app shell, target Chromium archive, macOS zip contents, Prisma engine, empty DB, and package resources without launching the GUI. `file release/mac/Hydra.app/Contents/MacOS/Hydra` reports `Mach-O 64-bit executable x86_64`, and `codesign --verify --deep --strict --verbose=2 release/mac/Hydra.app` reports `valid on disk` and `satisfies its Designated Requirement`. Notarization remains skipped without Apple credentials. |
| macOS Intel artifact | `release/Hydra-1.0.0-mac-x64.zip` exists locally at 300 MB with `release/Hydra-1.0.0-mac-x64.zip.blockmap` at 319 KB after the 2026-05-18 16:44 PDT local cross-package refresh. The current artifact is no longer stale relative to the 2026-05-18 native titlebar source change. |
| Packaged Chromium crash fix | Chromium ships as `Contents/Resources/chromium.zip` at 151 MB; package smoke rejects nested Chromium `.app` bundles under `Contents/Resources` and verifies target-specific Chromium archive children. |
| Codesign state | `release/mac-arm64/Hydra.app: valid on disk` and `satisfies its Designated Requirement`; notarization remains skipped without Apple credentials |
| Dependency audit | `npm_config_cache=/private/tmp/hydra-npm-cache npm audit --json` returned 0 vulnerabilities again on 2026-05-18 19:41 PDT after `npm audit fix` updated the Sentry/@fastify/otel nested `brace-expansion` copy from 5.0.5 to 5.0.6 in `package-lock.json`; `npm explain brace-expansion` confirms `node_modules/@fastify/otel/node_modules/brace-expansion@5.0.6` |
| Local secrets persistence fallback visibility | `server/services/local-secrets.js` keeps the owner-only temp-file, file `fsync`, atomic rename, and best-effort directory `fsync` persistence contract, and now logs warnings for unsupported/failed directory fsync, directory-handle close failures, temp-file close failures after write errors, and temp-file cleanup failures instead of hiding them. `server/tests/local-secrets.test.mjs` covers corruption quarantine/regeneration, atomic persistence shape, and fallback warning evidence; `npm run test:local-secrets`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 13:57 PDT. |
| Docker compose contract | `npm run docker:config` passed on 2026-05-16 07:45 PDT |
| Docker image build | Docker image construction passed on 2026-05-16 08:14 PDT after moving both Docker stages to `node:22-bookworm`; the rebuild eliminated the Electron 42 Node engine warnings and the Alpine/musl Prisma warning from the earlier attempt. The Dockerfile now also uses `npm ci --ignore-scripts` in both stages so server-image builds skip unnecessary Electron native rebuild postinstall work. |
| Bounded Docker smoke runner | `scripts/docker-smoke.mjs` replaced the shell-chained `docker:smoke` command with timed `docker compose config`, `docker info`, `docker compose build`, optional `--start` health polling for `/api/auth/status`, failed-start `compose ps -a` and log collection, and `docker compose down --remove-orphans` cleanup so Created containers are removed after failed start probes. `npm run test:docker-smoke` and `node --check scripts/docker-smoke.mjs` passed again on 2026-05-16 11:30 PDT; earlier `npm run docker:smoke -- --help` and `HYDRA_DOCKER_INFO_TIMEOUT_MS=5000 HYDRA_DOCKER_BUILD_TIMEOUT_MS=5000 npm run docker:smoke` ran on 2026-05-16 09:45-09:46 PDT, with the final command completing config and failing at `docker info` with the expected local daemon-unavailable error instead of hanging. |
| Intel Mac build path | `npm run electron:build:mac-x64` is the supported Intel macOS build path and should run on an Intel Mac or Intel macOS CI runner; target smoke must run with `HYDRA_BUILD_TARGET=darwin-x64`. |
| Windows build path | `release/Hydra-1.0.0-win-x64.exe` now exists at 290 MB with `release/Hydra-1.0.0-win-x64.exe.blockmap` at 309 KB; `release/win-unpacked/resources` contains the Windows Prisma engine (`app/node_modules/.prisma/client/query_engine-windows.dll.node`) and target `chrome-win` Chromium payload. The local macOS path used an isolated target cache staged from Playwright's Chrome-for-Testing `145.0.7632.6` Windows payload: `PLAYWRIGHT_BROWSERS_PATH=/private/tmp/hydra-pw-cross ELECTRON_CACHE=/private/tmp/hydra-electron-cache ELECTRON_BUILDER_CACHE=/private/tmp/hydra-electron-builder-cache npx electron-builder --win nsis --x64 -c.win.signAndEditExecutable=false`, then `HYDRA_BUILD_TARGET=win32-x64 npm run electron:smoke` passed again on 2026-05-18 19:42 PDT after smoke was tightened to require the installer blockmap and Windows Prisma engine. `file release/Hydra-1.0.0-win-x64.exe` reports a Nullsoft installer self-extracting archive, and `file release/win-unpacked/Hydra.exe` reports `PE32+ executable (GUI) x86-64`. The normal `npm run electron:build:win` path still belongs on a Windows runner/machine or a host with working Wine/rcedit; on this sandbox Wine fails with `wineserver: Can't check in server_mach_port` when `signAndEditExecutable` is enabled. |
| Workflow package/release, dev-port cleanup, and utility-probe contracts | `server/tests/workflow-contract.test.mjs` verifies the PR package-smoke workflow uses Node 22, runs `npm ci`, lint, full `npm test`, gate, build, `electron:prepare`, `electron-builder`, and `electron:smoke` across macOS arm64, macOS Intel, Windows x64 NSIS, and Linux AppImage; it also verifies the release workflow runs on `v*.*.*` tags, builds with `electron-builder --publish never`, smoke-checks the package, then uploads verified artifacts with `gh release upload`. It also locks down dev-port cleanup warning visibility, utility-probe failure visibility, target-specific package smoke, app-shell checks, artifact checks, LaunchServices packaged-app open preflight, packaged macOS native-window source checks, target-specific Prisma engine checks, and the Windows installer blockmap. |
| Packaged Electron dogfood runbook | `docs/PACKAGED_ELECTRON_DOGFOOD.md` now defines the final Electron-only manual/app-control dogfood checklist, requires `npm run electron:open:mac-arm64` instead of direct binary launch, rejects Chrome/`vite preview`/browser-only screenshots as blocker-closing evidence, keeps screenshot audit last, and includes the macOS window controls, navigation, live OTP, bulk OTP, redemption, proxy/SSE, Windows installer, Docker runtime, tray/menu, Touch ID, no-network, and screenshot evidence rows. `hydra audit --json` reports this as `packaged-dogfood-runbook`. |
| Cross-platform Chromium target selection | `electron:build:dmg`, `electron:build:win`, and `electron:build:linux` now set `HYDRA_BUILD_TARGET` before `electron:prepare`, so direct npm package scripts choose the target Chromium payload instead of the host payload; GitHub smoke/release workflows already pass the same env var through their matrix |
| Cross-platform package smoke | `scripts/smoke-electron-package.mjs` now lists `chromium.zip` with `unzip` on POSIX and PowerShell/.NET on Windows, so the Windows package-smoke runner does not depend on a Unix `unzip` binary |
| Cross-platform auxiliary-process cleanup | `electron/utils/cleanupAuxProcesses.js` now enumerates process command lines on Windows with `Get-CimInstance Win32_Process` and terminates matched Hydra auxiliary process trees with `taskkill.exe /PID <pid> /T /F` instead of no-oping on Windows. POSIX still uses `ps` plus `SIGTERM`. `electron/tests/main-process.test.mjs`, `node --check electron/utils/cleanupAuxProcesses.js`, `npm run lint`, and full `npm test` passed on 2026-05-16 10:03-10:05 PDT. |
| Multi-arch Docker publish path | Docker workflow publishes `linux/amd64` and `linux/arm64` |
| CI Node runtime and coverage | `package.json` declares `engines.node >=22.12`, and `.github/workflows/ci.yml`, `.github/workflows/electron-smoke.yml`, and `.github/workflows/release.yml` use `node-version: 22`, matching Electron 42's package-tooling requirement. CI now runs lint, tests, gate, and production build on every PR/master push, and Electron package smoke runs on every PR instead of only packaging-path changes. |
| Real Electron data preserved | `~/Library/Application Support/Hydra/hydra.db` currently contains 10 accounts, 11 management keys, and 11 keys; the earlier “No accounts stored” screen came from a temporary dev DB, not the real Electron data |
| Static UI contrast/chrome/navigation cleanup | `src/App.jsx` no longer renders the route title beside the Hydra app-chrome brand; `src/index.css` adds dark baselines for raw `input`/`textarea`/`select`/`.input`, autofill color hardening, stronger Traffic/page-header contrast, and removes stale `--bg-hover`; `server/tests/ui-static-contract.test.mjs` locks these regressions down and verifies every sidebar nav path is backed by a concrete route, with `/diagnostics` redirected into `Settings#diagnostics`; verified with lint/build/test/gate on 2026-05-16 05:17 PDT |
| Static dead-button guard | `server/tests/ui-static-contract.test.mjs` now scans active renderer pages/components for empty click handlers, hash-only links, `alert()` feedback, and visible "coming soon/not implemented" placeholder copy; `npm run test:ui-static` passed on 2026-05-16 09:20 PDT with 12 tests |
| First-run setup wizard | `src/App.jsx` now keeps first-time users in a guided setup flow after local password creation: optional OpenRouter management-key import via `api.addAccount()`, a compact launch tour, then Dashboard entry. This removes the setup/login ambiguity that produced "Invalid credentials" on fresh installs. `src/index.css` adds compact responsive setup-stepper/tour styling, and `server/tests/ui-static-contract.test.mjs` locks the password -> key -> tour -> Dashboard source contract. `npm run test:ui-static`, `npm run lint`, and `npm run build` passed on 2026-05-16 11:08 PDT. |
| Anime.js text-effect and app-shell polish | `src/components/AnimeText.jsx` uses Anime.js v4 `splitText().addEffect()` with cleanup via `splitter.revert()`, plus char, word, and line split modes. Dashboard now uses the new `signal` timeline variant with first/last stagger passes; scanline remains available for other page headers. `src/index.css` now gives the app shell a subtle grid/depth background, tighter sidebar surface, stable 44px nav rows, active-nav inset accent, constrained main-content width, and responsive page-header gaps without browser-only evidence claims. `server/tests/ui-static-contract.test.mjs` locks down the splitText/addEffect pattern, `signal` variant, page-header coverage, and shell polish. `npm run test:ui-static`, `npm run lint`, `npm run build`, `npm run gate`, and full `npm test` passed on 2026-05-18 19:25-19:38 PDT. |
| Account Generator compact layout | `src/pages/Generator.jsx` replaced the separate instruction card with compact inline step chips, and `src/index.css` constrains the generator to a single focused panel with responsive chips; static UI tests verify the old desktop-sized instruction panel does not return |
| Hydra API map | `npm run openapi:hydra` generated `docs/hydra-api.openapi.json` with 81 operations across 76 paths; `hydra api-map --tag accounts` and `hydra api-map --json --tag system` work while Hydra is closed; `docs/HYDRA_API_MAP.md` documents the private-app/Printing-Press-methodology boundary |
| CLI account detail | `hydra account 529c3bc9 --json` ran while Electron was closed and returned redacted detail with 25 stacked client cookies, 2 management-key records, and 3 API-key records without printing secret fields |
| CLI account metadata sync preflight | `hydra accounts sync --dry-run` checks which accounts have stored management keys before live OpenRouter balance/key metadata refresh; the `--yes` path is guarded and updates balance/key metadata without printing management-key material; `server/tests/cli.test.mjs` verifies keyed accounts are ready and password/OTP accounts without management keys are blocked |
| CLI code preflight | `hydra codes preflight TEST-CODE --json` ran while Electron was closed and returned 5 ready accounts and 7 blocked accounts without attempting live redemption |
| CLI key inventory | `hydra keys --json` ran while Electron was closed and returned 11 management-key rows without printing decrypted key material |
| CLI management-key provision preflight | `hydra keys provision <id> --dry-run` checks password, OTP, and already-keyed account states without live OpenRouter writes; the `--yes` path is guarded and stores created key material without printing the full key; `server/tests/cli.test.mjs` verifies `password_reauth`, `blocked`, and `already_has_management_key` outcomes against isolated data |
| CLI management-key rotation preflight | `hydra keys rotate <id> --dry-run` checks for a current management key plus a re-auth path before live replacement; missing confirmation returns `CONFIRMATION_REQUIRED`, keyed password accounts report `password_reauth`, and accounts without a current management key report `missing_management_key`; the `--yes` path is guarded and marks the previous local management-key row revoked only after a replacement key is stored |
| Management-key unreadable-row visibility | `server/services/management-key-store.js` now logs account/key-row context when duplicate management-key scans encounter an unreadable encrypted row, then continues scanning so one corrupt row does not block a valid dedupe/import path without evidence. `server/tests/management-key-backfill.test.mjs` covers legacy backfill idempotence plus this source contract; `npm run test:management-key-backfill`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 14:00 PDT. |
| CLI session inspection | `hydra session 529c3bc9 --json` ran while Electron was closed and returned an active stored session, 25 stacked client cookies, and a 2026-05-22 expiry without printing token values |
| CLI live session probe | `hydra session <id> --refresh --json` now uses `store.probeSessionLive()` to force the same explicit live session-check path as the UI while keeping session/client cookies redacted; `server/tests/cli.test.mjs` verifies the path against an isolated OTP account with no stored session material |
| CLI proxy status | `hydra proxy status --json` ran while Electron was closed and returned listener `running: false`, `gateEnabled: true`, masked proxy keys, and an explicit `warnings` array for degraded proxy key/gate metadata reads |
| CLI guarded proxy-key rotation | `hydra proxy keys new --yes --json` rotates the local proxy secret only after explicit confirmation and returns the new `sk-hydra-*`/`sk-proj-*` keys; without `--yes`, it returns `CONFIRMATION_REQUIRED`; `server/tests/cli.test.mjs` verifies this against an isolated temp data directory so real Hydra proxy keys are not touched |
| CLI quick scan | `hydra scan --quick --json` ran while Electron was closed and returned 12 accounts, 4 active stored sessions, 4 accounts with management keys, 4 immediately redemption-ready accounts, and 0 hard-blocked accounts |
| CLI redacted export | `hydra export --out /private/tmp/hydra-redacted-export.json --json` wrote a mode `0600` export with 12 accounts, 11 management-key rows, and 11 API-key rows; `rg` checks found no `sk-*` key-shaped strings or raw Clerk cookie assignments |
| CLI redacted import | `hydra import /private/tmp/hydra-import-check.json --dry-run --json` validates a redacted export without writing local data, and `hydra import <file> --yes --json` is now confirmation-gated metadata restore. The write path recreates account metadata and disabled/unpooled API-key metadata only, skips management-key secrets, restores no sessions/passwords/proxy keys/API-key plaintext, and reports `secretsRestored: 0`. `server/tests/cli.test.mjs` covers the dry-run validator, confirmation-required error, isolated metadata write, empty restored session token, null API-key plaintext, and zero restored management-key rows. |
| CLI reversible DB reset | `hydra db reset --dry-run --json` previews local `hydra.db`, `hydra.db-wal`, and `hydra.db-shm` moves, and `hydra db reset --yes --json` moves those files into `reset-backups/reset-<timestamp>` instead of deleting them. The command reports `deleted: 0`, requires confirmation, and is covered by an isolated temp-data-dir test that verifies active DB files are gone only because they were moved into the backup directory. |
| CLI closed-app server lifecycle | `hydra serve [--port N]` now starts `server/standalone.js` without Electron/Chrome/Vite, and `hydra stop [--port N] [--token <jwt>]` uses the locked `/api/shutdown` endpoint; unauthenticated stop attempts against a running listener return `AUTH_TOKEN_REQUIRED` instead of scraping session state or killing processes. Stop requests are bounded with `AbortSignal.timeout()`, non-JSON shutdown responses preserve a raw body preview for debugging, and timeout/request failures return explicit error codes instead of hanging. |
| CLI AI model catalog and chat | `hydra ai models --json` reads the local `CachedModel` table while Hydra is closed, supports `--filter` and `--limit`, and does not call OpenRouter or require proxy keys; `hydra ai chat --json` now calls a running local Hydra `/v1/chat/completions` proxy with explicit `--base-url`, `--key`, `--model`, `--max-tokens`, `--temperature`, and `--timeout-ms` controls, and returns `SERVER_UNAVAILABLE` with a `hydra serve` hint when the proxy is closed |
| CLI conservative account purge | `hydra accounts purge --dead --dry-run` previews only inert placeholder rows and `hydra accounts purge --dead --yes` deletes only rows with no email, no auth method, no stored password, no session material, no client-cookie stack, and no management key; `server/tests/cli.test.mjs` verifies the command keeps an OTP account with a reauth path |
| CLI log tailing | `hydra logs --lines N` now returns bounded snapshots, `hydra logs --json --tail` is rejected to avoid ambiguous streaming JSON, and `hydra logs --tail --lines N` follows appended log lines until interrupted |
| CLI unlock token issuance | `hydra unlock` verifies the local admin password and emits a bearer token for locked local endpoints; `server/tests/cli.test.mjs` seeds an isolated Prisma DB with a bcrypt admin password and verifies missing-password `PASSWORD_REQUIRED`, `--json`, `--token-only`, and wrong-password paths |
| CLI doctor diagnostics | `node bin/hydra.mjs doctor --json` returned concrete local checks on 2026-05-18 15:43 PDT: repo runtime data dir `$REPO/data`, DB present, local secrets present, recursive data-dir size, free disk, packaged Chromium zip present at `release/mac-arm64/Hydra.app/Contents/Resources/chromium.zip`, and port 3001 listener status. `server/tests/cli.test.mjs` now covers isolated `hydra doctor --json` output, top-level `doctor`/`data-dir` consistency with service-backed commands, and packaged Chromium zip resource detection. |
| CLI release audit command | `node bin/hydra.mjs audit --json` now separates active code-verifiable evidence from deferred manual GUI/runtime checks. Packaged GUI dogfood, live MVP dogfood, packaged screenshot audit, and Docker runtime smoke are reported as `deferred` after the user moved them out of active Codex scope. The command is read-only and inspects the goal sheet, release audit, package scripts, workflows, release artifacts, dependency-audit lockfile evidence, Docker docs, UI static contracts including first-run setup and app-shell polish, startup no-blank and splash greeting fallback contracts, Settings preference persistence contracts, native menu/tray feedback contracts, non-fatal fallback visibility contracts including store and legacy-storage fallback logging, redacted import metadata-write contracts, reversible DB reset contracts, Windows auxiliary-process cleanup, filesystem/migration-lock hardening, biometric fail-closed auth-token gating, and deferred manual items without launching Electron, Docker, browsers, or live OpenRouter/Clerk flows. `npm run test:cli` covers the active/deferred JSON shape. |
| CLI redaction, lifecycle, and OpenRouter regression coverage | `npm run test:cli` passed on 2026-05-18 19:45 PDT with 42 tests, including real-output checks for `hydra audit --json`, `hydra doctor --json`, top-level CLI data-dir consistency, packaged Chromium zip doctor detection, LaunchServices bundle-preflight audit evidence, `hydra account <id> --json`, conservative `hydra accounts purge --dead`, `hydra accounts sync --dry-run`, `hydra keys provision --dry-run`, `hydra keys rotate --dry-run`, `hydra session <id> --refresh --json`, `hydra ai models --json`, `hydra ai chat` closed-proxy, fake local `/v1` success behavior, and direct OpenRouter-compatible `--route direct` behavior, `hydra openrouter models/key/credits` against a fake OpenRouter-compatible API, `hydra proxy status --json`, guarded `hydra proxy keys new --yes --json`, `hydra scan --quick --json`, `hydra export --out <file> --json`, `hydra import <file> --dry-run --json`, guarded redacted metadata import via `hydra import <file> --yes --json`, reversible `hydra db reset --yes --json`, safe `hydra serve`/`hydra stop` lifecycle behavior, stop timeout/non-JSON shutdown source contracts, status warning-channel coverage, first-run setup, startup-fallback, Settings preference evidence, native menu-feedback evidence, fallback-visibility evidence, redacted-import evidence, reversible-db-reset evidence, active/deferred audit reporting, a spawned `hydra logs --tail` follow test, and `hydra unlock` token issuance. Direct OpenRouter model-cache writes are lazy-loaded only behind explicit `hydra openrouter models --cache`. |
| Prisma proxy bind-cache and disconnect visibility | `server/services/db.js` caches bound Prisma client methods per client instance, resets the cache on `disconnectPrisma()`, and now logs `[db] Prisma disconnect failed` when shutdown cleanup cannot disconnect cleanly instead of swallowing the failure. `server/tests/db-proxy-cache.test.mjs` is wired into `npm test` as `test:db-proxy-cache` and covers cache reuse, reset after successful disconnect, and warning evidence plus proxy reset after failed disconnect; `npm run test:db-proxy-cache`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 13:55 PDT. |
| Packaged runtime import cleanup | `electron/app/env.js` now consolidates packaged-runtime `node:fs` dynamic imports into a single import instead of re-importing `node:fs` inside the invalid-DB cleanup path; `server/tests/electron-data-path.test.mjs` guards this contract and `npm run test:electron-data-path` passed on 2026-05-16 07:25 PDT |
| Electron 42 package-tooling migration | `vite.config.js` now uses the function-form `manualChunks(id)` required by Vite/Rolldown 8, and `electron-builder.yml` uses electron-builder 26-compatible Linux desktop metadata plus no unsupported `win.publisherName`; the first builder retry proved the schema issue was fixed before the cache path was redirected to `/private/tmp` |
| Schema sync fallback visibility | `electron/app/schemaHash.js` now logs skipped migration stat reads, unreadable `.schema-mtimes` / `.schema-version` sentinel reads, and failed schema-sync checks before using fallback sync decisions. Missing sentinels remain quiet for first launch. `server/tests/schema-hash.test.mjs` covers hash caching, fatal fallback warning visibility, and unreadable-sentinel warning visibility; `npm run test:schema-hash`, `node --check electron/app/schemaHash.js`, and `node --check server/tests/schema-hash.test.mjs` passed on 2026-05-18 13:35 PDT. |
| Self-heal backup rotation and fallback visibility | `electron/app/schemaSelfHeal.js` now treats only root `hydra.db.backup-*` files as backup entries and prunes sidecars (`-wal`, `-shm`) together with their root, preventing sidecars from being counted as separate backups. WAL checkpoint failures, backup-prune failures, and migration-lock close/remove failures now log warnings while preserving non-fatal recovery behavior. `server/tests/schema-self-heal-backups.test.mjs` is wired into `npm test` as `test:schema-self-heal-backups`, and `npm run test:schema-self-heal-backups`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 13:51 PDT. |
| Electron URL allowlist hardening | `electron/app/env.js` normalizes bracketed IPv6 loopback hostnames and `electron/app/windows.js` uses the current app port when deciding whether to allow renderer window opens/navigation; `server/tests/electron-url-allowlist.test.mjs` verifies loopback HTTP(S) is allowed only on the expected app port and unsafe `VITE_DEV_SERVER_URL` values fall back, verified in `npm test` on 2026-05-16 05:38 PDT |
| Renderer native IPC result handling | Renderer callers now route native actions through `src/lib/native.js` helpers instead of raw `window.hydraNative` calls; ErrorBoundary window controls use `tryNative(native.minimizeWindow/closeWindow)`, and Settings/Diagnostics folder-open buttons catch native failures via `tryNative`; biometric-gated auth-token release now fails closed by calling `promptBiometric('Unlock Hydra')` whenever `biometricEnabled` is true and returning `null` on cancel, failure, or unavailable hardware instead of releasing the persisted token; Touch ID availability and prompt failures now log typed evidence instead of disappearing behind the fallback; `server/tests/electron-ipc-contract.test.mjs` verifies native IPC exposure, Result envelopes, renderer bridge usage, and the biometric fail-closed contract; `electron/tests/main-process.test.mjs` verifies biometric fallback visibility; `node --check electron/app/biometric.js`, `node --check electron/app/ipc.js`, `node --check electron/tests/main-process.test.mjs`, `node --check server/tests/electron-ipc-contract.test.mjs`, `npm run test:electron-ipc-contract`, and `npm run test:electron-main-process` passed on 2026-05-17 03:01 PDT |
| AppChrome native-control failure visibility | The Electron titlebar minimize/maximize/close controls now route through `tryNative()` instead of swallowing rejected native calls with empty catches; `npm run test:electron-ipc-contract`, `npm run test:ui-static`, `npm run lint`, and `npm run build` passed on 2026-05-16 09:08 PDT |
| TaskSupervisor cleanup failure visibility | `server/services/task-supervisor.js` now logs failed expiry sweeps, queue drains, resource closes, and custom cleanup errors instead of empty-catching them; `server/tests/task-supervisor.test.mjs` verifies cleanup failures release page/context/browser resources, archive the task error, and emit contextual warnings. `npm run test:task-supervisor`, `npm run lint`, and full `npm test` passed on 2026-05-16 09:10 PDT. |
| Background failure visibility | Dashboard balance-cache persistence, Dashboard session-status fallback, Account Generator Playwright cleanup/password-submit fallback, account bulk-dedup preload fallback, account silent-refresh fallback, pool status fallback, pool sync-key registration fallback, Debug vampire-mode profile preload fallback, magic-link cleanup timer lifecycle, request-log retention shutdown waits, corrupt-account purge failures, redemption-history alias/read/write failures, magic-link management-key auto-provision/opener notification failures, session lifetime probe failures, store session/account/key decrypt fallbacks, legacy storage unreadable-ciphertext reset probes, OpenRouter Playwright automation soft failures, CLI shutdown cleanup, Electron telemetry preference/crash-reporter/capture failures, auth-token/preference chmod failures, and OpenRouter 429 body-read failures remain non-fatal, but now emit contextual warnings/errors instead of disappearing behind empty catches. The management-key/code-redemption automation slices log overlay dismissal, copy/reveal clicks, clipboard permission grants, network logging, page-title/URL reads, iframe scans, DOM key-scan failures, browser clipboard-read denials, tracing/debug capture, server-action hash discovery/probe failures, forced-click retries for management-key form submission, profile/key-sync tRPC fallback failures, redeem tRPC outcome parse failures, redeem credits-preflight/account-lookup failures, and browser-close failures; redeem modal open/browser-close, redeem credit polling, redeem tRPC route persistence, and key-sync reveal/browser-close failures are logged too. `server/tests/background-failure-visibility.test.mjs` locks these contracts down with 18 tests, and `npm run test:background-failure-visibility`, `npm run test:cli`, relevant `node --check` commands, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-18 13:30 PDT. |
| Magic-link cleanup lifecycle | `server/services/magic-link-manager.js` no longer starts its expiry sweep timer at module import time. `server/index.js` starts cleanup only after the HTTP server listens, stops it during `gracefulShutdown()`, and the timer is `unref()`ed so closed-app commands and tests do not inherit hidden background work. `server/tests/background-failure-visibility.test.mjs`, `server/tests/electron-server-extract.test.mjs`, and `server/tests/electron-launch-compat.test.mjs` cover the source and launch contracts; `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:24 PDT. |
| Proxy RequestLog fallback visibility | `server/routes/proxy.js` now logs secondary RequestLog DB-write failures when the proxy retries usage logging without `keyHash`, so usage-log degradation preserves evidence for both the primary write failure and the anonymized fallback write failure. `server/tests/background-failure-visibility.test.mjs` covers this source contract; `npm run test:background-failure-visibility`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 14:03 PDT. |
| Proxy rotation selection fallback visibility | `server/services/rotation-manager.js` now validates per-key and total weighted-selection math, logs throttled warning evidence when malformed balance metadata forces a round-robin fallback, and keeps serving from eligible keys instead of silently skewing rotation. `server/tests/background-failure-visibility.test.mjs` covers the source contract; `node --check server/services/rotation-manager.js`, `node --check server/tests/background-failure-visibility.test.mjs`, `npm run test:background-failure-visibility`, and `npm run test:health-pinger` passed on 2026-05-17 03:07 PDT. |
| Proxy model-list fallback visibility | `/v1/models` still serves cached or static OpenAI-compatible model lists so SDK clients do not break, but live OpenRouter non-OK responses and cache/live lookup exceptions now log warning evidence before falling back to the static list. `server/tests/background-failure-visibility.test.mjs` covers the source contract; `node --check server/routes/proxy.js`, `node --check server/tests/background-failure-visibility.test.mjs`, `npm run test:background-failure-visibility`, and `npm run test:proxy-sse` passed on 2026-05-17 03:10 PDT. |
| Store local-state fallback visibility | `server/services/store.js` now logs account/key-scoped evidence for live session probe errors, stored session-token decrypt failures, uniqueness checks that skip unreadable account configs, first stored API-key decrypt failures, and local-key decrypt failures while keeping the existing non-fatal fallback behavior. `server/tests/background-failure-visibility.test.mjs` covers the source contract; `node --check server/services/store.js`, `node --check server/tests/background-failure-visibility.test.mjs`, `npm run test:background-failure-visibility`, and `npm run test:session-refresh-contract` passed on 2026-05-17 03:15 PDT. |
| OpenRouter request timeout and fallback visibility | `server/services/openrouter.js` now bounds direct OpenRouter account/key requests with `AbortSignal.timeout(30000)`, retries timeouts through the existing short retry ladder, and turns final timeouts into explicit request-timeout errors. It also logs credits and key-list lookup failures before returning safe zero/empty account snapshot defaults, so degraded OpenRouter metadata does not look like a real empty account without warning evidence. `server/services/model-cache.js` now bounds live model-list cache refreshes with the same 30s timeout class. `server/tests/background-failure-visibility.test.mjs` covers these source contracts; `node --check server/services/openrouter.js server/services/model-cache.js server/tests/background-failure-visibility.test.mjs`, `npm run test:background-failure-visibility` with 19 tests, `npm run test:cli`, `npm run lint`, `git diff --check`, and `node bin/hydra.mjs audit --json` passed on 2026-05-18 19:45 PDT. |
| Plan consolidation and route-title cleanup | Main plan docs were reconciled on 2026-05-18: Touch ID remains documented as implemented/source-tested but still needs packaged hardware dogfood; stale idea rows for first-run onboarding, tray live updates, startup error dialog, npm-audit vulnerabilities, and route titles were removed from `docs/IDEAS.md`; remaining CLI account-add/MCP ideas were consolidated there from the CLI plan. `src/App.jsx` now maps active routes to concrete document/window titles such as `Hydra — Pool Manager`, `Hydra — Account Detail`, and `Hydra — Diagnostics`; `server/tests/ui-static-contract.test.mjs` covers the route-title source contract. Unused `react-window` and `react-virtualized-auto-sizer` dependencies were removed after confirming no source imports. `npm run test:ui-static` passed with 19 tests, `npm run test:background-failure-visibility`, `npm run test:cli`, `npm run lint`, `npm run build`, full `npm test`, `git diff --check`, `npm audit --json`, and `node bin/hydra.mjs audit --json` passed on 2026-05-18 19:52-19:54 PDT. |
| Pool key validation response visibility | `server/controllers/PoolController.js` now treats malformed OpenRouter `/api/v1/auth/key` JSON as a visible 502 instead of silently accepting `null`, logs the selected key hash and parse cause, and logs hashless validation payloads before relying on the selected pool-row hash. `server/tests/background-failure-visibility.test.mjs` covers this source contract, and `npm run test:background-failure-visibility`, `node --check server/controllers/PoolController.js server/tests/background-failure-visibility.test.mjs`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:08 PDT. |
| Session lifetime probe failure visibility | `server/services/session-refresher.js` now logs account ids when stored session-token decrypt fails and when live refresh probes throw, instead of silently moving to no-sid/error states. `server/tests/background-failure-visibility.test.mjs` covers this source contract, and `npm run test:background-failure-visibility`, `npm run test:session-refresh-contract`, `node --check`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 11:55 PDT. |
| Renderer cleanup/history failure visibility | Account Generator job cleanup failures now log contextual warnings and surface warning toasts when the user is still in-app, while pagehide/beforeunload keepalive cleanup failures log warnings instead of disappearing. Code Redeemer history-load failures now clear/retry state, log a contextual warning, and render an in-page status message. `server/tests/ui-static-contract.test.mjs` locks both contracts down, and `npm run test:ui-static`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 09:20-09:22 PDT. |
| Renderer probe/copy failure feedback | Vault session probes and provisioning-readiness probes now log failed account ids and surface warning toasts instead of silently downgrading to `unknown`; Dashboard pool-sync/status probes, Account Detail live-session probes, and Pool Manager optional model/sync/proxy probes now log contextual warning evidence, with warning toasts for user-initiated provisioning/session checks that would otherwise fall back to stale state; Pool Manager copy failures log the failed copy target while keeping visible copy-failed button state; invalid non-JSON API responses preserve status, route, parse cause, and `INVALID_API_RESPONSE`, including malformed auth 401 payloads; Settings only shows copied state after either clipboard path actually succeeds; DevBackendHint exposes copied/copy-failed button state. `server/tests/ui-static-contract.test.mjs` locks these contracts down, and `npm run test:ui-static`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-18 13:42 PDT. |
| Diagnostics and key-modal feedback | Diagnostics now surfaces incomplete health/proxy refreshes, support-bundle clipboard failures, and native folder-open failures with visible status/toasts; Settings copy and folder-open actions now require real success before showing success state and expose failure reasons; CreatedKeyModal shows add-to-pool errors locally; RegisterKeyModal shows manual-paste guidance when clipboard read is blocked. `server/tests/ui-static-contract.test.mjs` and `server/tests/electron-ipc-contract.test.mjs` lock these contracts down, and `npm run test:ui-static`, `npm run test:electron-ipc-contract`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 09:40-09:41 PDT. |
| Settings preference persistence | Biometric and telemetry Settings toggles load through native `prefsGetAll`, write through native `prefsSet` before updating local UI state, and persist through Electron's owner-only `preferences.json` store. `server/tests/ui-static-contract.test.mjs` now covers the renderer/native bridge contract, `server/tests/user-prefs.test.mjs` covers cache-reset persistence and permissions, and `hydra audit --json` includes this as the `settings-prefs` release evidence item. `npm run test:ui-static`, `npm run test:user-prefs`, `npm run test:cli`, and `npm run lint` passed on 2026-05-16 11:18 PDT. |
| Electron main-process and path contracts wired into test chain | `npm run test:electron` now includes `electron/tests/main-process.test.mjs` and `electron/tests/path-allowlist.test.mjs`, covering main-process boot/shutdown wiring, full-quit routing through `shutdownEverything`, tracked child cleanup, auxiliary process/profile sweeps, tray destroy/state cleanup, persisted renderer auth-token IPC surface, native path allowlist rejection of symlink escapes, Help menu source contracts for docs/issues links, Diagnostics in Settings, logs/data folder actions, Build Info copy, and no-blank startup/activate fallback behavior. `npm run test:electron` passed on 2026-05-16 11:13 PDT after the startup fallback update. |
| Startup failure action feedback | `electron/app/startupError.js` now logs and surfaces Open Logs Folder failures, distinguishes Copy Details success from clipboard failure, and keeps the startup dialog recovery loop available after either action. `electron/tests/main-process.test.mjs` locks down the Open Logs / Copy Details buttons, user-visible failure messages, success message, contextual error logging, and absence of silent `catch { /* ignore */ }` blocks in the startup dialog helper. `node --check electron/app/startupError.js`, `node --check electron/tests/main-process.test.mjs`, and `npm run test:electron-main-process` passed on 2026-05-16 10:29 PDT. |
| Startup blank-window fallback | `electron/main.js` now keeps the startup fallback alive until either `ready-to-show` fires or a successful `loadURL()` proves the renderer loaded, then shows/focuses the main window if needed. The macOS `activate` path creates replacement windows hidden, shows only after `ready-to-show` or successful `loadURL()`, and closes a failed hidden window before showing the startup recovery dialog. `electron/tests/main-process.test.mjs` locks the no-blank startup/activate source contract; `node --check electron/main.js`, `node --check electron/tests/main-process.test.mjs`, `npm run test:electron-main-process`, `npm run test:electron`, and `npm run lint` passed on 2026-05-16 11:13 PDT. |
| Splash greeting fallback visibility | Splash greeting personalization still falls back from macOS full-name lookup to username and then empty greeting, but both fallback failures now log diagnostic evidence instead of disappearing during startup. `electron/tests/main-process.test.mjs` locks this source contract; `node --check electron/app/windows.js`, `node --check electron/tests/main-process.test.mjs`, `npm run test:electron-main-process`, and `npm run test:electron` passed on 2026-05-17 03:19 PDT. |
| Native menu/tray feedback | Tray folder opens and Help menu folder opens now await `shell.openPath()` and log OS failures, Build Info copy and Copy Proxy URL use a checked clipboard helper, and preload exposes a narrow `onMenuEvent` bridge so Copy Proxy URL success/not-ready and menu clipboard failures reach renderer toasts. `electron/tests/main-process.test.mjs`, `server/tests/electron-ipc-contract.test.mjs`, and `server/tests/ui-static-contract.test.mjs` lock down the main-process helpers, preload menu-event bridge, native wrapper, and renderer toasts, and `hydra audit --json` includes this as the `native-menu-feedback` release evidence item. `node --check` passed for `bin/commands/audit.js`, `server/tests/cli.test.mjs`, `electron/app/shutdown.js`, `electron/main.js`, `electron/menus/appMenu.js`, and `electron/preload.js`; `npm run test:electron-main-process`, `npm run test:electron-ipc-contract`, `npm run test:ui-static`, `npm run test:cli`, `npm run lint`, `npm run build`, and full `npm test` passed across the menu-feedback work, with the focused source/audit retest passing on 2026-05-16 11:21 PDT. |
| Electron startup/runtime best-effort visibility | `electron/app/env.js` now reports log rotation skips, log-stream write/close failures, packaged disk-space probe failures, and invalid DB backup/removal failures through warnings or startup summary errors. Log tee write failures use the raw pre-wrapper console warning channel and report only once, so a broken log stream cannot recurse or hide missing crash evidence. `electron/main.js` logs missing startup timing marks and uncaught-exception telemetry capture failures. `server/tests/electron-data-path.test.mjs` and `electron/tests/main-process.test.mjs` lock these contracts down, and `node --check electron/app/env.js`, `node --check electron/tests/main-process.test.mjs`, `npm run test:electron-main-process`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:28 PDT. |
| Legacy data migration integrity | `electron/utils/migrateLegacyData.js` now treats only `ENOENT`/`ENOTDIR` as missing path state, logs and skips migration on unreadable user/legacy paths, distinguishes a missing Account table from other Prisma inspection failures, logs Prisma disconnect cleanup failures, refuses to promote a legacy DB without an Account table, and copies non-DB sidecar files with `COPYFILE_EXCL` so newer userData files are not overwritten. `electron/tests/main-process.test.mjs` covers this source contract, and `npm run test:electron-main-process`, `node --check electron/utils/migrateLegacyData.js electron/tests/main-process.test.mjs`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:05 PDT. |
| Electron filesystem and secrets hardening | `server/lib/data-dir.js` creates the runtime data directory owner-only and now logs owner-only chmod repair failures instead of hiding them. `electron/app/schemaLock.js` creates the migration lock owner-only, breaks stale migration locks before acquiring, keeps a Windows PID-liveness path through `tasklist /FI "PID eq <pid>"`, and logs stale-lock unlink failures instead of hiding them. `server/services/local-secrets.js` writes `local-secrets.json` through an owner-only temp file, fsync, atomic rename, and best-effort directory fsync. `npm run test:filesystem-permissions`, `node --check server/lib/data-dir.js electron/app/schemaLock.js server/tests/filesystem-permissions.test.mjs`, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-16 16:39 PDT. |
| Diagnostics truth and test cleanup | Diagnostics are embedded at the bottom of Settings and show server facts from `GET /api/system/health`: `process.uptime()`, derived `startedAt`, `serverNow`, `process.pid`, pool counts, and upstream health; `server/tests/ui-static-contract.test.mjs` locks this down, and three tracked debug-only test files were removed to keep broad test discovery clean |
| Background health pinger cost reduction and upstream classification | `server/services/health-pinger.js` now validates pooled keys with `GET /api/v1/auth/key` instead of spending completion traffic on `POST /api/v1/chat/completions`; it selects available keys through `rotationManager.getNextKey()`, records successes through `rotationManager.recordSuccess()`, and shares the same upstream reachability classifier as `/api/system/health`, so OpenRouter 5xx responses mark upstream offline/degraded while 401/402/429 still prove reachability. `server/tests/upstream-health.test.mjs` and `server/tests/health-pinger-contract.test.mjs` cover this contract, and `npm run test:upstream-health`, `npm run test:health-pinger`, relevant `node --check` commands, `npm run lint`, `npm run build`, and full `npm test` passed on 2026-05-18 13:39 PDT. |
| CLI docs drift cleanup | `docs/HYDRA_CLI_AND_AI_API_PLAN.md` no longer claims Hydra has no CLI; it now distinguishes implemented closed-app inspection/lifecycle commands from planned account creation, live redemption, key rotation/provisioning, direct AI commands, and persistent unlock caching |
| Private API-map boundary documented | `docs/HYDRA_CLI_AND_AI_API_PLAN.md`, `docs/HYDRA_API_MAP.md`, `docs/CODEX_GOAL.md`, and `docs/hydra-api.openapi.json` now state that Hydra must not be uploaded to Printing Press, registered in a public CLI/library catalog, synced into a shared library, or generated as public endpoint tools; Printing Press is used only as a private methodology for source-derived API mapping, local OpenAPI output, and Hydra-native CLI design |
| Cleanup audit pass | `data/dev.db` was moved out of the runtime data directory to `/private/tmp/hydra-cleanup/data-dev.db-20260516`; `server/scripts/verify-fix.js` was removed as a dead ad hoc localhost verifier; `server/scripts/session-lifetime-probe.js` was retained as documented recon tooling; `scratch/` and `.scratch/` are absent; tracked `videos/` showreel/Remotion assets were audited and retained; `.gitignore` now covers temp/quarantine files, DB backups, local secrets temp files, and Playwright MCP captures |

## Not Yet Verified

These are no longer active Codex work items as of 2026-05-18 19:36 PDT. The
user explicitly moved packaged GUI dogfood, screenshot auditing, Intel launch,
and Windows launch gaps out of the plan because Electron GUI automation in this
environment is not trusted enough to close them. Keep this table as deferred
manual evidence, not as the current coding plan.

If the GUI scope is reopened later, the screenshot audit remains Electron-only.
Chrome, `vite preview`, localhost browser tabs, and browser-only CDP screenshots
are not representative of packaged Hydra and must not close visual, menu/tray,
persistence, or process-cleanup blockers.

| Requirement | Current blocker |
| --- | --- |
| Packaged GUI launch and macOS window controls after Chromium archive fix | User confirmed the packaged app can appear on screen, but also reported the visible window could not be closed, moved, dragged from the top bar, or inspected for native traffic lights, then provided a macOS crash report showing `EXC_CRASH (SIGABRT)` in `HIServices` `_RegisterApplication` with `Parent Process: node`. Source/package hardening restores native macOS titlebar traffic lights and drag affordance, and the bundle structure/signature checks pass; final packaged Electron dogfood still needs to confirm launch/window controls through LaunchServices via `npm run electron:open:mac-arm64`, not by spawning `Contents/MacOS/Hydra` directly. Fresh 2026-05-18 17:17 PDT retry: `npm run electron:open:mac-arm64` printed bundle preflight OK (`CFBundlePackageType=APPL`, `CFBundleExecutable=Hydra`, executable path present), confirmed the main executable is arm64 Mach-O, showed only `com.apple.provenance` on the bundle/executable and no `com.apple.quarantine`, verified codesign (`valid on disk`, `satisfies its Designated Requirement`), printed ad-hoc signature details, and then LaunchServices still returned `NSOSStatusErrorDomain Code=-10827 kLSNoExecutableErr`. `PlistBuddy` confirmed `Hydra`, `APPL`, and `com.zayd.hydra`. Computer Use `list_apps` did not show Hydra as running, and `get_app_state` for the packaged app was denied for `com.zayd.hydra`. |
| Local Docker daemon | Fresh `docker info --format '{{json .ServerVersion}}'` on 2026-05-18 17:14 PDT still failed locally: Docker client is present, but the user Docker socket under `~/.docker/run/docker.sock` is not accepting daemon connections. Docker Desktop is installed at `/Applications/Docker.app`, and its declared `CFBundleExecutable` (`com.docker.backend`) exists under `Contents/MacOS`, but `open -gj /Applications/Docker.app` previously returned the same LaunchServices `NSOSStatusErrorDomain Code=-10827 kLSNoExecutableErr` class seen when this sandbox tried to launch Hydra and Calculator, so Codex cannot start Docker Desktop from here. |
| Docker container start | Runtime container-start smoke still requires a host with Docker Desktop or Docker Engine running. The Docker smoke script is bounded and cleans up Created containers with `docker compose down --remove-orphans` after failed start probes. |
| Windows installer launch | `release/Hydra-1.0.0-win-x64.exe` now exists and package smoke passes, but actual install/launch dogfood still requires a Windows machine or Windows runner. The local macOS build path can create an unsigned/unedited NSIS artifact with a staged `chrome-win` cache and `-c.win.signAndEditExecutable=false`; the normal metadata/sign-edit path still needs Windows or working Wine because this sandbox's Wine exits with `wineserver: Can't check in server_mach_port`. |
| End-to-end OTP login | Requires live OpenRouter/Clerk account flow |
| Bulk OTP isolation | Requires live multi-account run |
| Session persistence after app quit/relaunch | Requires packaged GUI dogfood |
| Session expiry and re-auth UI | Unit/contract coverage exists; real expired-session UI dogfood still pending |
| Code redemption, single and bulk | Requires live redeemable code or controlled live-account test |
| Proxy rotation and SSE with real keys | SSE truncation source/regression coverage passes for clean completion, premature close, and upstream read error; real `/v1/chat/completions` rotation with live keys still pending |
| Tray close/reopen/quit and orphan process audit | Requires packaged GUI dogfood |
| Touch ID enable/disable/unlock | Requires packaged macOS GUI dogfood |
| Help menu link/copy actions | Source contract is covered by `electron/tests/main-process.test.mjs`; packaged GUI menu dogfood still required |
| No-network and kill-server recovery | Source/static contract covers backend-unavailable guidance, balanced loading state, and upstream-offline cached-local-data banner; controlled packaged GUI no-network/kill-server dogfood still pending |
| Visual page screenshot pass across sizes | Static CSS/App cleanup landed and build passes, but the required screenshot pass must be against the packaged Electron app. Browser or `vite preview` screenshots are source-only and do not reduce this blocker. A Playwright Electron probe on 2026-05-18 could not start in the current sandbox because Electron tried to create a cache under the user Library and hit `EPERM`; no visual verification was counted. |

## Current Release Commands

```bash
npm run electron:build
npm run electron:smoke
npm run electron:build:mac-x64       # run on Intel Mac or macos-15-intel
npm run electron:build:win           # run on Windows runner
npm run docker:smoke                 # requires Docker Desktop / Engine running
```
